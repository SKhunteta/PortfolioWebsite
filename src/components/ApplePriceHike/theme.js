// Apple-store minimalism: near-white canvas, near-black ink, one warning hue
// for the delta. Fonts lean on the system San Francisco stack so the piece
// reads like the page it's about.
export const SF =
  '-apple-system, "SF Pro Display", BlinkMacSystemFont, "Segoe UI", system-ui, sans-serif';
export const MONO = '"JetBrains Mono", "IBM Plex Mono", ui-monospace, monospace';

export const COLORS = {
  canvas: "#F5F5F7", // Apple light gray
  surface: "#FFFFFF",
  ink: "#1D1D1F", // Apple near-black
  muted: "#6E6E73", // Apple secondary text
  hairline: "#E3E3E6",
  before: "#6E6E73", // last week — quiet
  after: "#1D1D1F", // today — solid
  rise: "#B2261B", // the tax — a restrained Apple red
  riseSoft: "#FBEBE9",
};
